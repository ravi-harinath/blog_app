<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", type: "module" %>
  </head>

  <body class="bg-gray-100 min-h-screen flex flex-col">
    <nav class="bg-white shadow">
      <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
        <%= link_to "BlogApp", root_path, class: "text-xl font-bold text-blue-700 hover:text-blue-900" %>
        <%= link_to "All Posts", posts_path, class: "text-gray-700 hover:text-gray-900" %>
      </div>
    </nav>

  <% flash.each do |key, message| %>
  <div class="max-w-6xl mx-auto mt-4">
    <% if key == "notice" %>
      <div class="bg-green-100 border border-green-200 text-green-800 px-4 py-3 rounded">
        <%= message %>
      </div>
    <% elsif key == "alert" %>
      <div class="bg-red-100 border border-red-200 text-red-800 px-4 py-3 rounded">
        <%= message %>
      </div>
    <% else %>
      <div class="bg-gray-100 border border-gray-200 text-gray-800 px-4 py-3 rounded">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>


    <main class="flex-grow py-8">
      <%= yield %>
    </main>

    <footer class="bg-white shadow mt-8">
      <div class="max-w-6xl mx-auto px-4 py-4 text-sm text-gray-500">
        &copy; <%= Time.current.year %> BlogApp — Built with Rails & Tailwind CSS
      </div>
    </footer>
  </body>
</html>
